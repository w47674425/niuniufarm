# 牛牛农场 · 新手教程（3 分钟引导）

> 已集成进游戏本体，非独立页面。形态：游戏内交互式引导 + 随时可从「设置 → 🎓 新手教程」重看。

## 教什么（4 步，< 3 分钟）
| 步 | 操作 | 高亮目标 | 完成检测 |
|----|------|----------|----------|
| ① | 拖【牧民】到【蓝莓丛】→ 生产 | 牧民 + 蓝莓丛 | 两卡进入同一堆 |
| ② | 拖【面包】到【牧民】→ 喂饱 | 面包 + 牧民 | 触发喂食（饱食↑） |
| ③ | 拖【牛】到【市场】→ 卖钱 | 牛 + 市场 | 触发出售（金币↑） |
| ④ | 点【卡包】买一个卡包 | 底部卡包按钮 | 触发购买 |

每步自动检测完成、高亮目标卡、带「下一步✓」与「跳过教程」；做到即自动进入下一步。

## 触发时机
- **首次启动**：未完成过教程则弹「🎓 开始教程 / 直接玩」。
- **随时重看**：设置弹窗新增「🎓 新手教程」按钮。

## 关键设计纪律（对齐 GDD §4.1 / P1）
- 教程在**受控沙盘**运行，拦截 `saveGame`，绝不污染玩家真实存档。
- 结束教程时**优先 `loadGame` 还原玩家进度**，无档才开新局 → 随时重看也不丢档。
- 教程期间主循环只推进生产、冻结昼夜与怪物（`advanceTutorial`），时间/怪物/存档不动。

## 改动文件
- `src/tutorial.js`（新增）— 教程控制器：步骤定义、高亮 UI、检测、首尾逻辑。
- `src/systems.js` — `tick` 顶部加教程分支；`saveGame` 加 `tutorialActive` 拦截；`buyPack` 成功时通知教程。
- `src/drag.js` — 喂食 / 出售分支命中时 `notify` 教程。
- `src/modals.js` — 设置弹窗加「🎓 新手教程」入口；引入 `startTutorial`。
- `src/game.js` — 引入并启动 `maybeShowFirstLaunch`；引入 `startTutorial`。
- `src/styles.css` — 教程引导层样式（`.tut` / `.tut-ring` 脉冲高亮 / `.tut-panel`）。

## 验证
- `vite build` 通过（16 模块转译无错）。
- 未做真实浏览器 playtest；建议本地 `npm run dev` 跑一遍首启引导与 4 步检测。
- 待确认：沙盘卡布局在小屏（≤360px）是否拥挤、`bush` 采集 1 次即消失是否够直观。
